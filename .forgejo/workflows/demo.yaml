on: [push]
jobs:
  test:
    runs-on: nixos-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - run: nix --extra-experimental-features nix-command develop
      - run: just test
